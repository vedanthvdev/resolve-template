# Reference project analysis

Working notes from inspecting `fixtures/resolve_21/paris/paris.drp`.

## Status

Phase 1 scaffold. Drop a real Resolve 18+/21 export at the fixture path, then document:

- Archive layout (ZIP vs other)
- Project / timeline / media database filenames inside the archive
- What is stable across Resolve versions
- What must never be hand-edited

## Findings

_To be filled after `python3 -m resolve_template.cli inspect fixtures/resolve_21/paris/paris.drp`._
