# Phase 6 — Full story package

**Do not start until Phase 5 is green.**

## Goal

ZIP containing:

- native `.drp` only if Resolve exported it
- `Placeholder_Media/`
- shot tracker XLSX/CSV
- timeline map
- README that never lies about Resolve testing

## Gate

Relink by selecting `Placeholder_Media` once. Tracker rows match shot numbers and filenames.
