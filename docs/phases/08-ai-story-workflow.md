# Phase 8 — AI story workflow

**Do not start until Phases 2–6 are green.**

## Goal

Any model can emit `story.yaml`, call this CLI, and get a package. Creative planning stays outside Resolve XML.

## Files already started

- `schemas/story-v1.schema.json`
- `AI_INTEGRATION.md`
- `AGENTS.md`
- `prompts/RESOLVE_INVESTIGATOR.md`

## Gate

A second example story (not the one-clip POC) builds through the same Resolve-validated path.
